import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from "@storybook/addon-docs/blocks";
import { Button } from "../Button";
import { ButtonGroup } from "./ButtonGroup";

<Meta component={ButtonGroup} title="Components / Actions / Button group" />

# Button group

<Description of={ButtonGroup} />

<Preview withToolbar>
  <Story name="Example">
    <ButtonGroup>
      <Button>Save</Button>
      <Button outline>Invite and add another</Button>
    </ButtonGroup>
  </Story>
</Preview>

<Props of={ButtonGroup} />

## Usage

### Default button group

Use when you have multiple buttons to space them out evenly.
