import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from "@storybook/addon-docs/blocks";
import { useState } from "react";
import { Tabs } from "./Tabs";

<Meta component={Tabs} title="Components / Navigation / Tabs" />

# Tabs

<Description of={Tabs} />

<Preview withToolbar>
  <Story name="Example">
    {() => {
      const [selected, setSelected] = useState(0);
      const tabs = [
        { content: "Single invite", id: "single-invite" },
        { content: "Group invite", id: "group-invite" },
        { content: "Go to Envoy", id: "envoy", url: "https://envoy.com" },
      ];
      return <Tabs selected={selected} tabs={tabs} onSelect={setSelected} />;
    }}
  </Story>
</Preview>

<Props of={Tabs} />

## Usage

### Default tabs

Use for most cases, especially when the number of tabs may be more than three.

### Static tabs

Use when it's easier to define the tabs via JSX. Each tab item will be wrapped in a Tab
component and is responsible for providing an `onClick` handler that updates the parent
`selected` prop.

<Preview withToolbar>
  <Story name="Static tabs">
    {() => {
      const [selected, setSelected] = useState(0);
      return (
        <Tabs selected={selected} onSelect={setSelected}>
          <span>Tab created using a span</span>
          <Tabs.Tab>Tab created using Tabs.Tab</Tabs.Tab>
          <a href="https://envoy.com">
            Tab with external link with a regular anchor tag
          </a>
        </Tabs>
      );
    }}
  </Story>
</Preview>
