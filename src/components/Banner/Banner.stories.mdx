import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from "@storybook/addon-docs/blocks";
import { Banner } from "./Banner";

<Meta component={Banner} title="Components / Feedback indicators / Banner" />

# Banner

<Description of={Banner} />

<Preview withToolbar>
  <Story name="Example">
    <Banner status="warning" title="The header is optional">
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </Banner>
  </Story>
</Preview>

<Props of={Banner} />

## Usage

### Info

Use to update users about a change or give them advice.

<Story name="Info">
  <Banner status="info" title="The header is optional">
    <p>
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </p>
  </Banner>
</Story>

### Success

- Default to using toasts for success messages, unless the feedback is delayed,
  persistent, or has a call to action
- Include next steps if applicable

<Story name="Success">
  <Banner status="success" title="The header is optional">
    <p>
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </p>
  </Banner>
</Story>

### Warning

- Use to display information that needs attention or that users need to take action on
- Seeing these banners can be stressful for users so be cautious about using them

<Story name="Warning">
  <Banner status="warning" title="The header is optional">
    <p>
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </p>
  </Banner>
</Story>

### Optional header

Use when the banner body is self describing.

<Story name="Optional header">
  <Banner status="warning">
    <p>
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </p>
  </Banner>
</Story>
