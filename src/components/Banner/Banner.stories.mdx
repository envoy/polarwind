import {
  Meta,
  Story,
  Props,
  Description,
  Preview,
} from "@storybook/addon-docs/blocks";
import { Banner } from "./Banner";

<Meta title="Components / Feedback indicators / Banner" component={Banner} />

# Banner

<Description of={Banner} />

<Preview withToolbar>
  <Story name="Example">
    <Banner
      title="The header is optional"
      status="warning"
      children="This message box has a maximum width of 40em. The whole symbol should be
        no wider than 624px (text width 560px)."
    ></Banner>
  </Story>
</Preview>

<Props of={Banner} />

## Usage

### Info

Use to update users about a change or give them advice.

<Story name="Info">
  <Banner title="The header is optional" status="info">
    <p>
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </p>
  </Banner>
</Story>

### Success

- Default to using toasts for success messages, unless the feedback is delayed,
  persistent, or has a call to action
- Include next steps if applicable

<Story name="Success">
  <Banner title="The header is optional" status="success">
    <p>
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </p>
  </Banner>
</Story>

### Warning

- Use to display information that needs attention or that users need to take action on
- Seeing these banners can be stressful for users so be cautious about using them

<Story name="Warning">
  <Banner title="The header is optional" status="warning">
    <p>
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </p>
  </Banner>
</Story>

### Optional header

Use when the banner body is self describing.

<Story name="Optional header">
  <Banner status="warning">
    <p>
      This message box has a maximum width of 40em. The whole symbol should be
      no wider than 624px (text width 560px).
    </p>
  </Banner>
</Story>
