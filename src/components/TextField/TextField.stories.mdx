import {
  Meta,
  Story,
  Props,
  Description,
  Preview,
} from "@storybook/addon-docs/blocks";
import { TextField } from "./TextField";

<Meta title="Actions/TextField" component={TextField} />

# TextField

<Description of={TextField} />

<Preview withToolbar>
  <Story name="Text field">
    <TextField label="Name" required />
  </Story>
</Preview>

<Props of={TextField} />

# Usage

## With error

When the `error` prop is set, the field will display an error state. The error message
will take precedence over the `helpText` if set.

<Preview>
  <Story name="With error">
    <TextField label="Name" error="Name can't be empty" />
  </Story>
</Preview>
