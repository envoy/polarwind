import {
  Meta,
  Story,
  Props,
  Description,
  Preview,
} from "@storybook/addon-docs/blocks";
import { TextField } from "./TextField";

<Meta title="Forms/Text field" component={TextField} />

# Text field

<Description of={TextField} />

<Preview withToolbar>
  <Story name="Text field">
    <TextField label="Name" required helpText="Help me out" />
  </Story>
</Preview>

<Props of={TextField} />

# Usage

## With error

When the `error` prop is set, the field will display an error state. The error message
will take precedence over the `success` and `helpText` if set.

<Preview>
  <Story name="With error">
    <TextField
      label="Name"
      error="Name can't be empty"
      success="Saved"
      helpText="Help me out"
      required
    />
  </Story>
</Preview>

## With success

When the `success` prop is set, the field will display the success message below the
field. The success message will take precedence over the `helpText` if set.

<Preview>
  <Story name="With success">
    <TextField label="Name" success="Saved" helpText="Help me out" />
  </Story>
</Preview>
