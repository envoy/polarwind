import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from "@storybook/addon-docs/blocks";
import { DeliveriesEmptyState } from "../../assets";
import { EmptyState } from "./EmptyState";

<Meta component={EmptyState} title="Components / Structure / Empty state" />

# Empty state

<Description of={EmptyState} />

<Preview withToolbar>
  <Story name="Example">
    <div style={{ backgroundColor: "#f6f6f9", padding: "1rem" }}>
      <EmptyState heading="No data yet..." image={DeliveriesEmptyState}>
        <p>
          When you start scanning deliveries, analytics will help you identify
          trends to improve operations and scale mail operations. Track total
          deliveries, time to pick-up, top recipients, and more.
        </p>
      </EmptyState>
    </div>
  </Story>
</Preview>

<Props of={EmptyState} />

## Usage

### Default empty state

Use to explain a single feature before users have used it.

### No header

(Don't actually use this. This story is here to test the component with edge cases to
ensure the layout still holds.)

<Preview withToolbar>
  <Story name="No header">
    <div style={{ backgroundColor: "#f6f6f9", padding: "1rem" }}>
      <EmptyState image={DeliveriesEmptyState}>
        <p>
          When you start scanning deliveries, analytics will help you identify
          trends to improve operations and scale mail operations. Track total
          deliveries, time to pick-up, top recipients, and more.
        </p>
      </EmptyState>
    </div>
  </Story>
</Preview>

### No copy

(Don't actually use this. This story is here to test the component with edge cases to
ensure the layout still holds.)

<Preview withToolbar>
  <Story name="No copy">
    <div style={{ backgroundColor: "#f6f6f9", padding: "1rem" }}>
      <EmptyState
        heading="No data yet..."
        image={DeliveriesEmptyState}
      ></EmptyState>
    </div>
  </Story>
</Preview>

### Short copy

(Don't actually use this. This story is here to test the component with edge cases to
ensure the layout still holds.)

<Preview withToolbar>
  <Story name="Short copy">
    <div style={{ backgroundColor: "#f6f6f9", padding: "1rem" }}>
      <EmptyState heading="No data yet..." image={DeliveriesEmptyState}>
        <p>Just start scanning.</p>
      </EmptyState>
    </div>
  </Story>
</Preview>

### Just the image

(Don't actually use this. This story is here to test the component with edge cases to
ensure the layout still holds.)

<Preview withToolbar>
  <Story name="Just the image">
    <div style={{ backgroundColor: "#f6f6f9", padding: "1rem" }}>
      <EmptyState image={DeliveriesEmptyState} />
    </div>
  </Story>
</Preview>
