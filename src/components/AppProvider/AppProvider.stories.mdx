import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from "@storybook/addon-docs/blocks";
import { withSmartKnobs } from "storybook-addon-smart-knobs";
import { Page } from "../Page";
import { AppProvider } from "./AppProvider";

<Meta
  component={AppProvider}
  decorators={[withSmartKnobs({ ignoreProps: ["children"] })]}
  title="Components / Structure / App provider"
/>

# App provider

<Description of={AppProvider} />

<Preview withToolbar>
  <Story name="Example">
    <AppProvider>
      <Page title="Hello" />
    </AppProvider>
  </Story>
</Preview>

<Props of={AppProvider} />

## Usage

### Default

AppProvider works by default without any additional options passed to it.
